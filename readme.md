## cosi-testing

This project is a l´playground to get myself familier with the cosi system.

1. Int controller creates new int resources on a timer and delete's the int resources if the corresponding string resource get's deleted
2. String controller creates matchign string resources for all int resources
3. 